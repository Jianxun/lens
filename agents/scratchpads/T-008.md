# T-008 — Sessions API & persistence
## Context
- Sessions list in UI; messages belong to exactly one session, linear by creation time.
- Only finalized messages are persisted (superseded drafts not stored).
- Session CRUD per ADR-0006.

## Scope / Allowed files
- `backend/api/sessions.py`
- `backend/models/`
- `backend/main.py`

## Requirements
- Endpoints: list/get/create/patch/delete (soft archive). Support pin/rename; archive hides from default list.
- Persist session_messages with idx and uniqueness; maintain linear order.
- Ensure messages linked once; no multi-session messages.

## DoD
- CRUD works in manual tests; session_messages reflect order; archived sessions hidden by default.

