# T-012 — Orchestrator tool trace UX

## Task summary
- **DoD**
  1. Emit structured trace of every tool call (peek/turn) with inputs and timing; persist per-chat run for frontend consumption.
  2. Frontend: add right-side collapsible panel showing all peek traces (histogram + query string) without overlapping main chat; provide histogram plot + trace list.
  3. Ensure traces load lazily/toggle without blocking chat; keep layout responsive.
- **Verify**
  1. `python scripts/orchestrator_smoke.py "test trace"` logs tool-call traces with queries and histogram payloads.
  2. `cd frontend && npm run dev` → manual chat shows collapsible right panel with histogram + peek query list; toggle hides/shows without shifting chat input.

## Read
- agents/context/lessons.md — confirmed standing instructions (date command, no Docker, load .env for embeddings)
- agents/context/contract.md — boundaries + verification protocol
- agents/context/tasks.md — task board + DoD/verify for T-012
- agents/context/handoff.md — prior work + branch context
- backend/services/agent.py — orchestrator + metadata plumbing
- backend/api/chat.py — streaming endpoint wiring
- scripts/orchestrator_smoke.py — CLI verify entrypoint
- frontend/src/App.tsx & App.css — existing UI layout + histogram panel
- frontend/src/types.ts — Chat metadata/types
- frontend/src/api.ts — SSE handler

## Plan
1. **Backend trace capture** — Extend orchestrator tool execution to timestamp each call, include arguments + responses, and surface traces via AgentService metadata + CLI smoke logging.
2. **API/type propagation** — Ensure `/chat` streaming metadata (Python + TS types + SSE handler) carries serialized trace payloads for the latest run.
3. **Frontend trace panel** — Redesign chat layout to support a right-side collapsible panel (histogram plot + peek trace list) fed by metadata, removing the old bottom histogram block and keeping interactions responsive.

## Progress log
- 2025-12-26 01:46 EST — Reviewed lessons/contract/tasks/handoff; created scratchpad + marked T-012 In Progress; inventoried backend/ frontend files relevant to traces + histogram UI.

## Patch summary
- _TODO_

## Verification
- _TODO_

## Blockers / Questions
- _None_

## Next steps
1. Await Architect/user review of the proposed plan; adjust as needed before coding.
